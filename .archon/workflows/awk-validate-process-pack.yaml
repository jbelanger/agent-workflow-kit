name: awk-validate-process-pack
description: |
  Validate the agent-workflow-kit Archon workflow pack wiring without invoking an AI node.

worktree:
  enabled: false

nodes:
  - id: validate
    bash: "node scripts/validate-archon-pack.mjs"
